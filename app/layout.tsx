import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title:
    "Vipin Garg Finance Coaching | Professional Finance Coaching for Exams & Academic Excellence",
  description:
    "Professional finance coaching for CFA, Canadian Securities Institute exams, and university finance programs. Structured exam strategy with real-world financial insight by Vipin Garg.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans`}>{children}</body>
    </html>
  );
}
